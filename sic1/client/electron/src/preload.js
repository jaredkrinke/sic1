const greenworks = require("greenworks");

if (greenworks.init()) {
    // TODO: This should translate between SIC-1's WebView2-based `Platform` abstraction and Greenworks
} else {
    // TODO: Abort?
}
