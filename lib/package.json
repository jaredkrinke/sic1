{
  "name": "sic1asm",
  "version": "0.0.1",
  "description": "Single Instruction Computer, Mark 1 (SIC-1) assembler and emulator",
  "main": "sic1asm.js",
  "scripts": {
    "test": "mocha --require ts-node/register test/*.spec.ts",
    "watch:test": "npx mocha --watch --require ts-node/register test/*.spec.ts --watch --watch-extensions ts",
    "build": "copy /y package.json dist && tsc -p .",
    "watch": "tsc -w -p ."
  },
  "author": {
    "name": "Jared Krinke"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^5.2.7",
    "@types/node": "^13.1.8",
    "mocha": "6",
    "ts-node": "^8.6.2",
    "typescript": "^3.7.5"
  },
  "files": [
    "sic1asm.js",
    "sic1asm.d.ts"
  ]
}
